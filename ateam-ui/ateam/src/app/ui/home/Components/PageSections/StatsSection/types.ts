interface StatsSectionProps {
    top: number;
}

interface StatParallaxProps {
    statName: string;
    initialValue: number;
    initialX: number;
    initialY: number;
    textColor: string;
    shadowColor: string;
}
